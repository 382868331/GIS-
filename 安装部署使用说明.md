# 点云上传平台：安装、部署与使用说明

本文档对应本仓库当前版本，介绍如何在本地或 Linux 服务器上部署完整的 LAS 点云平台。

平台由以下组件组成：

- React + TypeScript + Three.js 前端；
- FastAPI 后端；
- PostgreSQL 业务数据库；
- Redis 登录会话；
- MinIO 原始 LAS 对象存储；
- Nginx 静态文件、API 与 WebSocket 反向代理。

> 内置示例 `NEONDSSampleLiDARPointCloud.las` 由 Git LFS 管理。克隆仓库后必须执行 `git lfs pull`，否则只能得到一个 LFS 指针文件。

## 1. 已实现的主要功能

- 登录/注册一体化演示认证；
- 记住账号密码、用户协议和隐私政策；
- LAS 分片上传、暂停、继续和刷新后断点续传；
- 同名文件重复上传，每次生成独立 UUID，不覆盖旧文件；
- MinIO 保存 LAS，PostgreSQL 保存对象键和业务元数据；
- 自动提取 CRS、EPSG、分类统计、回波统计、GPS 时间范围和 VLR/EVLR；
- Three.js 点云预览、旋转、平移和缩放；
- 加点、画线、画面、测距和文字标注；
- 编辑自动保存、手动保存、撤销和重做；
- WebSocket 实时通知；
- 原始 LAS 流式导出；
- 用户无数据时自动初始化仓库内置示例；
- 后端单元测试和真实 LAS 回归脚本。

## 2. 服务器配置建议

### 2.1 临时 MVP 演示

- 2 核 CPU；
- 2GB 内存；
- 40GB SSD；
- 4GB Swap；
- 单人或极少量用户；
- 同一时间只进行一个大文件上传或解析任务。

该配置可以用于笔试、录屏和短时间演示，但上传或解析大型 LAS 时可能出现明显变慢。不要在该配置上保存大量生产数据。

### 2.2 推荐演示配置

- 4 核 CPU；
- 8GB 内存；
- 100GB 以上 SSD；
- 50Mbps 以上公网带宽。

### 2.3 正式单机配置

- 8 核 CPU；
- 16GB 内存；
- 500GB～1TB NVMe SSD；
- 100Mbps 以上公网带宽；
- 独立异机备份。

Three.js 在访问者浏览器中渲染，服务器不需要 GPU。

## 3. 端口规划

| 端口 | 服务 | 是否开放公网 |
|---:|---|---|
| 22 | SSH | 仅可信 IP |
| 80 | HTTP | 是 |
| 443 | HTTPS | 是 |
| 8000 | FastAPI | 否，仅本机 |
| 5432 | PostgreSQL | 否，仅本机 |
| 6379 | Redis | 否，仅本机 |
| 9000 | MinIO API | 否，仅本机 |
| 9001 | MinIO 控制台 | 否，按需 SSH 隧道访问 |

阿里云安全组只需开放 `22`、`80` 和 `443`。不要把数据库、Redis 或 MinIO 直接暴露到公网。

## 4. Ubuntu 服务器部署

以下命令以 Ubuntu 24.04 LTS、部署目录 `/opt/gis-pointcloud`、域名 `gis.example.com` 为例。请将域名替换成自己的域名。

### 4.1 安装基础软件

```bash
sudo apt update
sudo apt install -y \
  git git-lfs curl ca-certificates nginx redis-server \
  postgresql postgresql-contrib python3 python3-venv python3-pip

git lfs install
```

安装 Docker：

```bash
curl -fsSL https://get.docker.com | sudo sh
sudo systemctl enable --now docker
sudo usermod -aG docker "$USER"
```

执行 `usermod` 后重新登录 SSH，使 Docker 用户组生效。

安装 Node.js 22：

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node --version
npm --version
```

### 4.2 2GB 内存服务器创建 Swap

如果服务器只有 2GB 内存，强烈建议创建 4GB Swap：

```bash
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
free -h
```

### 4.3 克隆代码和拉取示例 LAS

```bash
sudo mkdir -p /opt/gis-pointcloud
sudo chown "$USER":"$USER" /opt/gis-pointcloud

git clone git@github.com:382868331/GIS-.git /opt/gis-pointcloud
cd /opt/gis-pointcloud
git lfs pull
```

验证示例不是 LFS 指针：

```bash
ls -lh NEONDSSampleLiDARPointCloud.las
```

正常大小约为 177MiB（185,075,623 字节）。

### 4.4 创建 PostgreSQL 数据库

先生成数据库密码：

```bash
openssl rand -hex 24
```

进入 PostgreSQL：

```bash
sudo -u postgres psql
```

执行以下 SQL，并替换密码：

```sql
CREATE USER pointcloud WITH ENCRYPTED PASSWORD '替换为数据库强密码';
CREATE DATABASE pointcloud OWNER pointcloud;
\q
```

数据库只监听本机即可，不要修改为公网监听。

### 4.5 配置 Redis

启用并确认 Redis：

```bash
sudo systemctl enable --now redis-server
redis-cli ping
```

正确结果为：

```text
PONG
```

Redis 只监听 `127.0.0.1`。2GB 服务器可以在 `/etc/redis/redis.conf` 中设置：

```conf
maxmemory 128mb
maxmemory-policy allkeys-lru
```

修改后重启：

```bash
sudo systemctl restart redis-server
```

### 4.6 配置后端环境变量

```bash
cd /opt/gis-pointcloud
cp api/.env.example api/.env
openssl rand -hex 32
openssl rand -hex 32
openssl rand -hex 32
```

编辑配置：

```bash
nano api/.env
```

生产示例：

```dotenv
DATABASE_URL=postgresql+psycopg://pointcloud:数据库密码@127.0.0.1:5432/pointcloud
ASYNC_DATABASE_URL=postgresql+asyncpg://pointcloud:数据库密码@127.0.0.1:5432/pointcloud
REDIS_URL=redis://127.0.0.1:6379/0

AUTH_SECRET=替换为openssl生成的随机值
AUTH_COOKIE_SECURE=true

MINIO_ROOT_USER=pointcloud-root
MINIO_ROOT_PASSWORD=替换为独立的MinIO根密码
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=pointcloud-api
MINIO_SECRET_KEY=替换为独立的MinIO应用密钥
MINIO_BUCKET=point-clouds
MINIO_SECURE=false
MINIO_REGION=us-east-1

UPLOAD_TEMP_DIR=data/upload-parts
LEGACY_UPLOAD_DIR=data/uploads
BUNDLED_SAMPLE_PATH=../NEONDSSampleLiDARPointCloud.las

MAX_UPLOAD_SIZE_BYTES=536870912
UPLOAD_CHUNK_SIZE_BYTES=5242880
UPLOAD_SESSION_HOURS=24
MAX_PREVIEW_POINTS=200000
```

注意：

- `api/.env` 已被 Git 忽略，不要提交；
- 三个密码/密钥应分别生成，不要重复使用；
- 2GB 内存服务器建议把 `MAX_PREVIEW_POINTS` 改为 `100000`；
- 修改 MinIO 密钥后必须使用同一份 `api/.env` 启动 MinIO。

### 4.7 启动 MinIO

```bash
cd /opt/gis-pointcloud
docker compose -f infra/minio/compose.yaml up -d
docker compose -f infra/minio/compose.yaml ps
```

查看日志：

```bash
docker compose -f infra/minio/compose.yaml logs --tail=100 minio
docker compose -f infra/minio/compose.yaml logs --tail=100 minio-init
```

`minio` 应为 `healthy`，`minio-init` 正常完成后可以处于 `Exited (0)`。

MinIO 使用 Docker 命名卷 `point-cloud-minio-data` 持久化。停止服务但保留文件：

```bash
docker compose -f infra/minio/compose.yaml down
```

不要随意增加 `-v`，否则会删除 MinIO 数据卷。

如需访问 MinIO 控制台，可在本地电脑建立 SSH 隧道：

```bash
ssh -L 9001:127.0.0.1:9001 用户名@服务器IP
```

然后打开 `http://127.0.0.1:9001`。

### 4.8 安装后端依赖

```bash
cd /opt/gis-pointcloud/api
python3 -m venv .venv
./.venv/bin/python -m pip install --upgrade pip
./.venv/bin/python -m pip install -r requirements.txt
```

首次手动启动验证：

```bash
cd /opt/gis-pointcloud/api
./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
```

后端启动时会：

1. 检查 MinIO 和存储桶；
2. 创建数据库表；
3. 执行兼容性迁移；
4. 初始化通知与会话组件。

确认无错误后按 `Ctrl+C` 停止。

### 4.9 创建 systemd 后端服务

```bash
sudo tee /etc/systemd/system/pointcloud-api.service >/dev/null <<'EOF'
[Unit]
Description=Point Cloud Platform FastAPI
After=network.target postgresql.service redis-server.service docker.service
Requires=postgresql.service redis-server.service docker.service

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=/opt/gis-pointcloud/api
Environment=PYTHONUNBUFFERED=1
ExecStart=/opt/gis-pointcloud/api/.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
Restart=on-failure
RestartSec=5
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
EOF
```

如果服务器登录用户名不是 `ubuntu`，修改 `User` 和 `Group`。

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now pointcloud-api
sudo systemctl status pointcloud-api
```

查看实时日志：

```bash
journalctl -u pointcloud-api -f
```

2GB 内存服务器只能使用 `--workers 1`。更高配置也建议先使用一个 Worker，因为当前 WebSocket 连接中心保存在进程内；需要多 Worker 时应先把实时广播改为 Redis Pub/Sub。

### 4.10 构建前端

生产环境建议让浏览器和 API 使用同一域名，由 Nginx代理 `/api`。

```bash
cd /opt/gis-pointcloud/web
cp .env.example .env.production
nano .env.production
```

填写完整公网地址：

```dotenv
VITE_API_BASE_URL=https://gis.example.com
```

没有域名、暂时只用服务器 IP 和 HTTP 时：

```dotenv
VITE_API_BASE_URL=http://服务器公网IP
```

安装和构建：

```bash
cd /opt/gis-pointcloud/web
npm install -g pnpm@11
pnpm install --frozen-lockfile
pnpm build
```

构建结果位于 `/opt/gis-pointcloud/web/dist`。生产服务器不需要运行 Vite开发服务。

### 4.11 配置 Nginx

```bash
sudo tee /etc/nginx/sites-available/pointcloud >/dev/null <<'EOF'
server {
    listen 80;
    server_name gis.example.com;

    client_max_body_size 520m;

    root /opt/gis-pointcloud/web/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/notifications/ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }
}
EOF

sudo ln -s /etc/nginx/sites-available/pointcloud /etc/nginx/sites-enabled/pointcloud
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

如果只使用 IP，把 `server_name gis.example.com;` 改成：

```nginx
server_name _;
```

### 4.12 配置 HTTPS

域名已经解析到服务器后：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d gis.example.com
sudo certbot renew --dry-run
```

HTTPS部署必须保持：

```dotenv
AUTH_COOKIE_SECURE=true
VITE_API_BASE_URL=https://gis.example.com
```

修改后重启后端并重新构建前端：

```bash
sudo systemctl restart pointcloud-api
cd /opt/gis-pointcloud/web
pnpm build
sudo systemctl reload nginx
```

## 5. 本地开发

### 5.1 Windows

准备 PostgreSQL、Redis、Docker Desktop、Python 和 Node.js。

```powershell
git clone git@github.com:382868331/GIS-.git
cd GIS-
git lfs install
git lfs pull

Copy-Item api\.env.example api\.env
docker compose -f infra\minio\compose.yaml up -d

cd api
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

另开一个终端：

```powershell
cd web
npx --yes pnpm@11 install
npx --yes pnpm@11 dev
```

打开 `http://127.0.0.1:5173`。

### 5.2 Linux/macOS

```bash
cp api/.env.example api/.env
docker compose -f infra/minio/compose.yaml up -d

cd api
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt
./.venv/bin/python -m uvicorn app.main:app --reload
```

另开终端：

```bash
cd web
npx --yes pnpm@11 install
npx --yes pnpm@11 dev
```

## 6. 平台使用流程

1. 打开平台；
2. 输入邮箱和至少 8 位密码；
3. 无账号时自动注册，有账号时直接登录；
4. 当前演示模式下密码错误会先更新密码再登录；
5. 首次登录且没有 LAS 记录时，自动把内置示例写入 MinIO 和 PostgreSQL；
6. 首页自动展示用户最早的一条点云；
7. 点击底部“上传”按钮选择 LAS；
8. 上传期间可以暂停、继续或刷新页面后重新选择同一文件续传；
9. 点击“文件”按钮切换当前展示的 LAS；
10. 点击“工具箱”使用点、线、面、测距和文字标注；
11. 编辑内容会自动保存，也可以点击“保存”手动保存；
12. 点击“导出”下载当前原始 LAS；
13. 通知按钮展示上传、编辑、保存、删除和导出记录。

## 7. 验证部署

### 7.1 服务状态

```bash
sudo systemctl status postgresql
sudo systemctl status redis-server
sudo systemctl status pointcloud-api
sudo systemctl status nginx
docker compose -f /opt/gis-pointcloud/infra/minio/compose.yaml ps
```

### 7.2 API 和页面

```bash
curl -I http://127.0.0.1
curl -I http://127.0.0.1:8000
```

API根路径需要登录 Cookie，因此未登录返回 `401` 是正常行为。浏览器登录后可以打开：

```text
https://gis.example.com/docs
```

### 7.3 自动测试

后端单元测试：

```bash
cd /opt/gis-pointcloud/api
./.venv/bin/python -m pytest
```

前端：

```bash
cd /opt/gis-pointcloud/web
pnpm build
pnpm lint
```

真实 LAS 回归：

```bash
cd /opt/gis-pointcloud
api/.venv/bin/python scripts/run_regression.py \
  --api http://127.0.0.1:8000 \
  --sample NEONDSSampleLiDARPointCloud.las
```

回归脚本会验证认证、断点续传、MinIO写入、PostgreSQL记录、元数据、预览、下载哈希、自动保存、手动保存、通知和删除。

## 8. 更新版本

```bash
cd /opt/gis-pointcloud
git pull --ff-only
git lfs pull

cd api
./.venv/bin/pip install -r requirements.txt
sudo systemctl restart pointcloud-api

cd ../web
pnpm install --frozen-lockfile
pnpm build
sudo nginx -t
sudo systemctl reload nginx
```

更新前建议先备份 PostgreSQL 和 MinIO。

## 9. 备份与恢复

### 9.1 PostgreSQL备份

```bash
mkdir -p /opt/backups/pointcloud
PGPASSWORD='数据库密码' pg_dump \
  -h 127.0.0.1 -U pointcloud -Fc pointcloud \
  > /opt/backups/pointcloud/database-$(date +%F-%H%M).dump
```

恢复到空数据库：

```bash
PGPASSWORD='数据库密码' pg_restore \
  -h 127.0.0.1 -U pointcloud -d pointcloud \
  --clean --if-exists 备份文件.dump
```

### 9.2 MinIO备份

不要只备份 PostgreSQL。数据库只保存对象键，原始 LAS 在 MinIO 中。

最简单的单机演示备份方式是停止写入后备份 Docker 卷：

```bash
docker run --rm \
  -v point-cloud-storage_point-cloud-minio-data:/source:ro \
  -v /opt/backups/pointcloud:/backup \
  alpine tar czf /backup/minio-$(date +%F-%H%M).tar.gz -C /source .
```

正式环境应将备份复制到另一台服务器或云对象存储，不能只放在同一块系统盘。

## 10. 常见故障

### 10.1 上传时后端退出

检查是否被系统 OOM Kill：

```bash
dmesg -T | grep -Ei 'out of memory|killed process'
journalctl -u pointcloud-api -n 200 --no-pager
free -h
```

处理建议：

- 创建 4GB Swap；
- FastAPI只使用一个 Worker；
- `MAX_PREVIEW_POINTS` 降为 `100000`；
- 同一时间只上传一个大文件；
- 升级到至少 4GB 内存。

### 10.2 页面一直显示正在验证身份

```bash
sudo systemctl status pointcloud-api
journalctl -u pointcloud-api -n 100 --no-pager
curl -I http://127.0.0.1:8000
```

同时确认：

- 前端 `VITE_API_BASE_URL` 与实际访问域名一致；
- Nginx `/api/` 代理正确；
- HTTPS下 `AUTH_COOKIE_SECURE=true`；
- HTTP调试时必须使用 `AUTH_COOKIE_SECURE=false`。

### 10.3 WebSocket连接失败

确认 Nginx 已为 `/api/notifications/ws` 配置：

- HTTP/1.1；
- `Upgrade`；
- `Connection "upgrade"`；
- 足够长的读写超时。

浏览器从 HTTPS页面访问时必须使用 `wss://`，不能使用 `ws://`。

### 10.4 MinIO启动失败

```bash
docker compose -f infra/minio/compose.yaml ps
docker compose -f infra/minio/compose.yaml logs --tail=200 minio
docker compose -f infra/minio/compose.yaml logs --tail=200 minio-init
```

确认：

- `api/.env` 存在；
- 密钥不是空值；
- 9000和9001端口没有被其他进程占用；
- Docker磁盘空间充足。

### 10.5 内置示例无法初始化

```bash
cd /opt/gis-pointcloud
git lfs pull
ls -lh NEONDSSampleLiDARPointCloud.las
```

确认 `BUNDLED_SAMPLE_PATH=../NEONDSSampleLiDARPointCloud.las`，并且后端工作目录为 `/opt/gis-pointcloud/api`。

### 10.6 磁盘空间不足

```bash
df -h
docker system df
du -sh /opt/gis-pointcloud/api/data
```

不要直接删除 MinIO Docker 卷。先从平台删除无用文件，并确认数据库记录和对象存储同步。

## 11. 安全注意事项

- 不要提交 `api/.env`；
- 不要在前端代码中写数据库或 MinIO 密钥；
- 不要开放 5432、6379、9000、9001到公网；
- 生产环境必须使用 HTTPS；
- 定期轮换数据库、认证和 MinIO 密钥；
- 定期验证备份可以恢复；
- 演示模式允许自动注册和重置密码，不适合作为正式生产认证；
- 正式上线前应关闭“密码错误自动重置”逻辑，增加邮箱验证、管理员授权、限流和审计策略。

## 12. 当前范围说明

当前版本暂未实现：

- Potree、EPT 或 3D Tiles 大规模 LOD；
- 多节点 MinIO 高可用；
- 前后端完整 Docker Compose；
- CI自动验证流水线。

当前预览采用服务端抽样后由 Three.js一次性渲染，适合 MVP 和中等规模演示；超大规模生产点云应增加离线转换与按相机位置加载的 LOD 管线。
