# 简易点云上传平台功能需求

## 一、项目目标

实现一个前后端分离的简易点云上传平台，完成以下业务闭环：

> 用户上传 LAS 点云文件 → 后端校验并保存文件 → 数据库记录上传信息 → 前端展示上传记录 → 用户在浏览器中查看三维点云

建议技术栈：

- 前端：React + TypeScript；
- 后端：FastAPI；
- 数据库：SQLite + SQLAlchemy；
- LAS 解析：laspy + NumPy；
- 三维展示：Three.js + OrbitControls；
- 自动化测试：pytest。

---

## 二、必须完成的功能需求

### 1. 点云文件上传

- 用户可以在网页中选择一个 `.las` 文件；
- 用户可以点击“上传”按钮提交文件；
- 前端使用 `multipart/form-data` 将文件发送到后端；
- 上传期间显示上传中状态；
- 上传成功后显示成功提示；
- 上传失败后显示具体错误原因；
- 上传过程中禁止重复点击上传按钮；
- 上传成功后自动刷新点云列表。

### 2. 前端文件基础检查

- 未选择文件时不能提交；
- 前端只允许选择 `.las` 文件；
- 对大写扩展名 `.LAS` 也应正确识别；
- 文件超过规定大小时给出提示；
- 前端检查只用于改善交互，不能替代后端检查。

### 3. 后端 LAS 文件有效性校验

- 后端必须再次检查文件扩展名；
- 后端必须检查文件是否为空；
- 后端必须检查文件大小是否超过限制；
- 后端读取文件头，确认文件签名为 `LASF`；
- 使用 laspy 实际解析 LAS 文件；
- 解析失败时拒绝上传；
- 普通文本文件改名为 `.las` 后必须被拒绝；
- 损坏或被截断的 LAS 文件必须被拒绝；
- 校验失败时返回明确的错误状态和错误信息；
- 校验失败后不能生成数据库记录；
- 校验失败后不能在上传目录中残留文件。

### 4. 点云文件保存

- 通过校验的 LAS 文件保存在服务器文件系统中；
- 禁止把 LAS 文件二进制直接存入数据库；
- 使用 UUID 等唯一标识生成服务器文件名；
- 不能直接使用用户上传的文件名作为服务器文件名；
- 保留用户原始文件名用于页面展示；
- 文件存储目录通过配置指定；
- 代码中不能出现与个人电脑绑定的绝对路径；
- 同名文件不能互相覆盖；
- 数据库写入失败时，应清理已经保存的文件。

建议目录：

```text
data/
└── uploads/
    └── {uuid}.las
```

### 5. LAS 元数据提取

上传成功后，后端需要从 LAS 文件中读取并记录：

- LAS 版本；
- 点记录格式；
- 点数量；
- 是否包含 RGB；
- X、Y、Z 坐标范围；
- 文件大小；
- 原始文件名；
- 上传时间。

可以额外记录：

- SHA-256；
- scale；
- offset；
- 软件标识；
- 系统标识。

### 6. 数据库记录

建立点云记录表，至少包含以下字段：

```text
id
original_name
storage_key
size_bytes
las_version
point_format
point_count
has_rgb
status
created_at
updated_at
```

建议增加以下字段：

```text
sha256
min_x
max_x
min_y
max_y
min_z
max_z
error_message
```

数据库要求：

- 上传成功后必须生成记录；
- 文件保存位置只记录相对路径或存储键；
- 数据库中不能存储 LAS 二进制内容；
- 数据库密码或连接信息不能直接写死在代码中；
- 数据库表应通过初始化脚本或迁移脚本创建。

### 7. 点云记录列表

前端需要展示用户已经上传的点云记录。

列表至少显示：

- 原始文件名；
- 文件大小；
- 点数量；
- LAS 版本；
- 是否包含 RGB；
- 处理状态；
- 上传时间；
- “查看”按钮。

列表还需要具备：

- 页面打开时自动加载记录；
- 上传成功后刷新记录；
- 没有记录时显示空状态；
- 查询失败时显示错误提示；
- 加载过程中显示加载状态。

### 8. 点云详情

- 用户可以点击列表中的“查看”按钮；
- 页面根据点云记录 ID 查询对应数据；
- 展示文件名、大小、点数量、LAS 版本和上传时间；
- 如果记录不存在，显示明确的错误页面；
- 如果点云尚未处理完成，显示处理中状态；
- 如果点云处理失败，显示失败原因。

### 9. 点云预览数据接口

后端需要提供浏览器可使用的点云预览数据。

接口需要完成：

- 根据记录 ID 查找 LAS 文件；
- 验证数据库记录和磁盘文件都存在；
- 使用 laspy 读取点坐标；
- 正确应用 LAS 中的 scale 和 offset；
- 读取点的 RGB 数据；
- 对大量点进行抽样；
- 返回点坐标和颜色；
- 设置最大预览点数，防止浏览器崩溃；
- 文件不存在或读取失败时返回明确错误。

建议最大预览点数：

```text
200,000～500,000 点
```

预览数据可以使用：

- JSON：实现简单，适合小型演示；
- 二进制：传输效率更高，属于较好实现；
- Potree 格式：适合大点云，属于增强方案。

### 10. 三维点云展示

前端必须在浏览器中真正渲染点云，不能只显示截图或下载链接。

查看器必须支持：

- 显示点云；
- RGB 颜色渲染；
- 鼠标旋转；
- 鼠标滚轮缩放；
- 鼠标平移；
- 自动计算点云包围盒；
- 自动将相机定位到点云；
- 显示加载状态；
- 加载失败时显示错误信息。

建议使用：

```text
Three.js
BufferGeometry
THREE.Points
OrbitControls
```

### 11. 无 RGB 点云处理

并非所有 LAS 文件都包含 RGB，因此必须处理这种情况：

- 有 RGB 时使用原始 RGB 渲染；
- 没有 RGB 时不能导致页面报错；
- 没有 RGB 时使用统一颜色、高程或强度着色；
- 页面提示“该点云不包含 RGB，当前使用其他方式着色”。

### 12. 大坐标和显示精度处理

- 读取原始坐标后计算点云中心；
- 渲染前将坐标转换为相对于中心点的局部坐标；
- 避免建筑或城市坐标数值过大导致点云抖动；
- 保留原始坐标信息用于详情展示；
- 相机应根据点云尺寸自动设置合适的近裁剪面和远裁剪面。

### 13. 后端 API

至少实现以下接口：

#### 上传点云

```http
POST /api/point-clouds
```

#### 查询点云列表

```http
GET /api/point-clouds
```

#### 查询点云详情

```http
GET /api/point-clouds/{id}
```

#### 获取点云预览数据

```http
GET /api/point-clouds/{id}/preview
```

接口要求：

- 使用统一的数据结构；
- 使用正确的 HTTP 状态码；
- 业务错误返回可读的错误信息；
- 不能把服务器绝对路径返回给前端；
- FastAPI 自动生成的 Swagger 文档可以正常使用。

### 14. 错误处理

至少需要处理：

- 未上传文件；
- 文件扩展名错误；
- 空文件；
- 文件过大；
- 伪造 LAS；
- LAS 文件损坏；
- LAS 解析失败；
- 数据库写入失败；
- 文件保存失败；
- 点云记录不存在；
- 数据库记录存在但磁盘文件不存在；
- 点云预览失败；
- 前端网络请求失败。

前端应显示用户能理解的提示，后端不能直接暴露：

- 服务器异常堆栈；
- 数据库密码；
- 服务器绝对路径；
- 系统内部敏感信息。

### 15. 自动化测试

至少为一个后端接口或核心业务逻辑编写自动化测试。

最低要求：

- 测试能够通过一条命令运行；
- 测试不依赖个人电脑上的绝对路径；
- 测试运行后能够自动清理临时文件和测试数据库。

建议至少实现以下测试：

1. 合法 LAS 上传成功；
2. 普通文本改名为 `.las` 后上传失败；
3. 损坏 LAS 上传失败；
4. 上传失败后没有数据库记录；
5. 上传失败后没有残留文件；
6. 查询点云列表成功；
7. 查询不存在的点云返回 `404`。

### 16. 配置管理

- 使用环境变量或配置文件管理运行参数；
- 提供 `.env.example`；
- 真实 `.env` 不提交到代码仓库；
- 文件上传目录可配置；
- 最大上传大小可配置；
- 数据库地址可配置；
- 前端后端地址可配置；
- 代码中不能写真实密码、密钥；
- 代码中不能写个人环境的绝对路径。

### 17. README 文档

README 至少包含：

- 项目简介；
- 已实现功能；
- 技术选型；
- 技术选型理由；
- 项目目录结构；
- 环境要求；
- 安装方法；
- 配置方法；
- 数据库初始化方法；
- 前端启动方法；
- 后端启动方法；
- 完整演示流程；
- API 说明；
- 前后端主要数据流；
- 数据库设计；
- 文件存储设计；
- LAS 校验方式；
- 点云预览实现方式；
- 自动化测试运行方法；
- 已完成的测试；
- 已知问题；
- 技术取舍；
- 后续改进方向；
- 项目中采用的业务假设。

### 18. 项目演示

提供演示视频或可访问的演示地址。

演示必须包含：

1. 打开系统；
2. 选择一个合法 LAS 文件；
3. 上传文件；
4. 上传成功后在列表中看到记录；
5. 点击查看；
6. 浏览器显示点云；
7. 演示 RGB 渲染；
8. 演示旋转；
9. 演示缩放；
10. 演示平移。

建议额外演示：

- 上传普通文本伪装的 `.las` 文件；
- 系统提示文件不是有效 LAS；
- 无 RGB 点云的回退显示。

---

## 三、建议完成的功能

以下功能原题没有逐项强制要求，但实现后项目会更加完整。

### 19. 上传进度

- 显示文件上传百分比；
- 显示正在上传的文件名；
- 上传完成或失败后结束进度状态；
- 防止用户在上传期间重复提交。

### 20. 点云列表分页

- 支持页码；
- 支持每页条数；
- 后端返回总记录数；
- 默认按上传时间倒序排列。

### 21. 下载原始文件

- 用户可以下载原始 LAS 文件；
- 下载文件名使用原始文件名；
- 不能允许用户通过参数访问任意服务器文件；
- 下载接口根据数据库中的存储键查找文件。

### 22. 删除点云

- 用户可以删除上传记录；
- 删除前要求二次确认；
- 同时删除数据库记录、原始文件和预览文件；
- 文件删除失败时不能静默忽略；
- 删除成功后自动刷新列表。

### 23. 点云查看器控制项

- 调节点大小；
- 修改背景颜色；
- 在 RGB、高程、强度和统一颜色之间切换；
- 重置相机；
- 显示当前点数量；
- 显示点云坐标范围。

### 24. 重复文件识别

- 上传时计算 SHA-256；
- 数据库记录文件哈希；
- 检测到相同文件时给出提示；
- 可以选择拒绝重复上传或允许生成新的业务记录。

### 25. 数据库迁移

- 使用 Alembic 管理数据库结构；
- 提供初始化迁移；
- README 中写明迁移命令；
- 不依赖手动创建数据库表。

---

## 四、可选加分功能

### 26. 用户注册和登录

**原题没有要求登录和注册，不属于必须功能。**

如果时间充足，可以实现：

- 用户注册；
- 用户登录；
- 用户退出；
- 密码加密存储；
- JWT 或 Session 身份认证；
- 未登录用户不能上传；
- 用户只能看到自己上传的点云；
- 用户只能查看、下载和删除自己的点云。

如果实现登录系统，需要增加：

```text
users 表
point_clouds.user_id 字段
```

不要为了实现登录注册而耽误 LAS 校验和三维展示。

### 27. 异步点云处理

- 上传完成后创建后台处理任务；
- 数据库状态从 `PROCESSING` 变为 `READY`；
- 处理失败变为 `FAILED`；
- 前端轮询或通过 WebSocket 获取处理状态；
- 大文件解析不会长时间阻塞上传接口。

### 28. 对象存储

- 使用 MinIO、S3 等对象存储保存原始 LAS；
- 数据库只保存对象键；
- 支持预签名上传和下载；
- 本地开发仍提供简单启动方法。

### 29. 大规模点云分块加载

- 将 LAS 转换为 Potree、EPT 或 3D Tiles；
- 根据相机位置加载不同层级；
- 支持 LOD；
- 避免一次性加载全部点；
- 显示转换和加载进度。

### 30. Docker 一键运行

- 前端提供 Dockerfile；
- 后端提供 Dockerfile；
- 提供 Docker Compose；
- 一条命令启动前端、后端和数据库；
- 数据文件通过 Volume 持久化。

### 31. CI 自动验证

- 提交代码后自动安装依赖；
- 自动运行后端测试；
- 自动构建前端；
- 测试或构建失败时给出明确结果。

---

## 五、页面清单

### 必须页面

1. 点云上传和列表页面；
2. 点云详情及三维查看页面。

### 可选页面

3. 登录页面；
4. 注册页面；
5. 个人中心页面；
6. 系统错误页面。

---

## 六、开发优先级

建议按照以下顺序实现：

1. 创建前端和后端项目；
2. 创建数据库模型；
3. 实现 LAS 上传接口；
4. 实现 LAS 有效性校验；
5. 实现文件保存和数据库记录；
6. 实现点云列表接口；
7. 实现前端上传页面；
8. 实现前端点云列表；
9. 实现预览数据接口；
10. 实现 Three.js 三维查看器；
11. 处理无 RGB 点云；
12. 增加错误处理；
13. 编写自动化测试；
14. 完成 README；
15. 录制演示视频；
16. 最后再考虑登录、删除、对象存储等增强功能。

---

## 七、最小合格交付范围

时间有限时，至少完成：

- React 前端；
- FastAPI 后端；
- SQLite 数据库；
- `.las` 文件上传；
- 后端真实 LAS 校验；
- 本地文件保存；
- 数据库元数据记录；
- 点云记录列表；
- 点云预览接口；
- Three.js 三维展示；
- RGB 渲染；
- 旋转、缩放和平移；
- 至少一个自动化测试；
- README；
- 演示视频或演示地址。

登录注册、删除、Docker、对象存储和异步任务均不是原题硬性要求。
